{
  "buildCommand": "echo 'No build needed for static site'",
  "outputDirectory": "vercel-backend",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/vercel-backend/$1"
    }
  ]
}
